# 第十阶段学习计划：数据库、缓存与消息队列

## 阶段目标

完成这一阶段后，你应该能围绕“数据库、缓存与消息队列”独立创建 Go 项目、手敲示例代码、运行验证命令，并用自己的话复盘关键概念。

本阶段的代码文件由你自己在 GoLand 中创建。本仓库只预先提供这份学习计划。

## 准备确认

开始前确认：

- 已使用 GoLand 创建或打开 `study_10` 目录。
- 已准备在 `study_10` 目录下初始化 Go module。
- 能在 GoLand Terminal 中执行 Go 命令。

## 需要你手动创建的文件

- `go.mod`
- `main.go`
- `config.example.json`
- `README.md`

## 学习重点

- database/sql
- MySQL 基础访问
- Redis 基础访问
- 消息队列生产与消费
- 配置管理
- 连接生命周期

## 执行步骤

### 1. 使用 GoLand 创建 study_10 Go 项目，或打开 study_10 目录后执行 go mod init study_10

完成标志：

```text

```

### 2. 准备 config.example.json，记录需要的连接信息字段

完成标志：

```text

```

### 3. 使用 database/sql 连接 MySQL 并完成最小查询

完成标志：

```text

```

### 4. 使用参数化 SQL 完成一条插入或查询

完成标志：

```text

```

### 5. 使用 Redis 客户端完成字符串读写

完成标志：

```text

```

### 6. 选择一种消息队列完成最小生产者和消费者示例

完成标志：

```text

```

### 7. 在程序结束时关闭连接或释放资源

完成标志：

```text

```

### 8. 把无法本地运行的外部服务要求记录清楚

完成标志：

```text

```

## 本阶段不展开

以下内容只需知道名字，暂不深入：

- 框架封装
- 大型项目结构
- 复杂并发或性能优化
- 生产级错误处理规范

## 复盘清单

完成后，用自己的话回答：

- database/sql 中的 DB 是单个连接吗？

```text

```

- 为什么 SQL 参数要使用占位符？

```text

```

- Redis 在本阶段练习里承担什么角色？

```text

```

- 消息队列生产者和消费者分别负责什么？

```text

```

- 连接生命周期为什么需要明确管理？

```text

```

如果其中某个问题说不清楚，可以先提问。需要长期保留的问题，再沉淀到 `知识问答` 目录。