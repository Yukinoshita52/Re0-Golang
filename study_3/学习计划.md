# 第三阶段学习计划：数组、切片、映射与字符串

## 阶段目标

完成这一阶段后，你应该能围绕“数组、切片、映射与字符串”独立创建 Go 项目、手敲示例代码、运行验证命令，并用自己的话复盘关键概念。

本阶段的代码文件由你自己在 GoLand 中创建。本仓库只预先提供这份学习计划。

## 准备确认

开始前确认：

- 已使用 GoLand 创建或打开 `study_3` 目录。
- 已准备在 `study_3` 目录下初始化 Go module。
- 能在 GoLand Terminal 中执行 Go 命令。

## 需要你手动创建的文件

- `go.mod`
- `main.go`
- `main_test.go`

## 学习重点

- 数组和切片
- append、len、cap
- map 增删改查
- 字符串、byte、rune
- strings 与 strconv

## 执行步骤

### 1. 使用 GoLand 创建 study_3 Go 项目，或打开 study_3 目录后执行 go mod init study_3

完成标志：

```text

```

### 2. 创建 main.go，手敲数组和切片的定义、遍历和 append

完成标志：

```text

```

### 3. 编写切片求和、筛选和去重函数

完成标志：

```text

```

### 4. 使用 map 完成单词统计

完成标志：

```text

```

### 5. 分别按 byte 和 rune 遍历含中文字符串

完成标志：

```text

```

### 6. 使用 strings 和 strconv 完成简单转换练习

完成标志：

```text

```

### 7. 创建 main_test.go，为切片或 map 处理函数写测试

完成标志：

```text

```

## 本阶段不展开

以下内容只需知道名字，暂不深入：

- 框架封装
- 大型项目结构
- 复杂并发或性能优化
- 生产级错误处理规范

## 复盘清单

完成后，用自己的话回答：

- 数组和切片的核心区别是什么？

```text

```

- len 和 cap 分别表示什么？

```text

```

- append 后一定还是同一个底层数组吗？

```text

```

- 读取 map 中不存在的 key 会得到什么？

```text

```

- 为什么处理中文字符串时要关注 rune？

```text

```

如果其中某个问题说不清楚，可以先提问。需要长期保留的问题，再沉淀到 `知识问答` 目录。